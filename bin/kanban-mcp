#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
REPO_ROOT="$(cd "${SCRIPT_DIR}/.." && pwd)"
export PYTHONUNBUFFERED=${PYTHONUNBUFFERED:-1}
exec python3 "${REPO_ROOT}/mcp_server.py" "$@"
